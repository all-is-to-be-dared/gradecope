#!/bin/bash

echo "TODO"

# need to generate server keypair + CA keypair + runner keypair
# then just need to to give server its keypair + CA pubkey, and the runner just needs its own
# keypair and the server keypair
